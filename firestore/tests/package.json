{
  "name": "moonbase-firestore-rules-tests",
  "private": true,
  "description": "Standalone @firebase/rules-unit-testing suite for MoonBase firestore.rules",
  "scripts": {
    "test": "npx firebase emulators:exec --project demo-moonbase --only firestore --config ../../firebase.json \"npx jest --runInBand\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.6.0",
    "firebase-admin": "^13.2.0",
    "firebase-tools": "^14.0.1",
    "jest": "^29.7.0"
  }
}
