{
  "name": "moonbase-storage-rules-tests",
  "private": true,
  "description": "Standalone @firebase/rules-unit-testing suite for MoonBase storage.rules",
  "scripts": {
    "test": "npx firebase emulators:exec --project demo-moonbase --only storage --config ../../firebase.json \"npx jest --runInBand\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.6.0",
    "firebase-tools": "^14.0.1",
    "jest": "^29.7.0"
  }
}
